export const validMahasiswa = {
  username: process.env.MAHASISWA_USER ?? 'mahasiswa',
  password: process.env.MAHASISWA_PASS ?? 'siasisten',
};

export const validDosen = {
  username: process.env.DOSEN_USER ?? 'dosen',
  password: process.env.DOSEN_PASS ?? 'siasisten',
};

export const validAdmin = {
  username: process.env.ADMIN_USER ?? 'admin',
  password: process.env.ADMIN_PASS ?? 'siasisten',
};

